{
  "name": "mirror-fit",
  "version": "0.1.0",
  "private": true,
  "description": "MirrorFit Chrome extension MVP scaffold",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.321",
    "esbuild": "^0.25.4",
    "typescript": "^5.8.3"
  }
}
